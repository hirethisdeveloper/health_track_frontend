import React, {Component} from 'react';
import { Button } from 'reactstrap';

class EditReadingButton extends Component {
    render() {
        return (
            <Button>Edit</Button>
        );
    }
}

export default EditReadingButton;