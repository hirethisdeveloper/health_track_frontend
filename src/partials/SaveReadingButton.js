import React, {Component} from 'react';
import { Button } from 'reactstrap';

class SaveReadingButton extends Component {
    render() {
        return (
            <Button>Save</Button>
        );
    }
}

export default SaveReadingButton;