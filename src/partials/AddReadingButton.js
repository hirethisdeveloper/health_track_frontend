import React, {Component} from 'react';
import { Button } from 'reactstrap';

class AddReadingButton extends Component {
    render() {
        return (
            <Button>Add</Button>
        );
    }
}

export default AddReadingButton;